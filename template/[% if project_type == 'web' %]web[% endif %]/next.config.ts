import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack is the default builder in Next.js 16 for dev and build.
  // Keep this skeleton minimal; add domains, redirects, and headers as the app grows.
  reactStrictMode: true,
};

export default nextConfig;

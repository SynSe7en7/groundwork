# GitHub Copilot instructions

The canonical project instructions live in [`AGENTS.md`](../AGENTS.md) at the
repository root. Read that file first and follow it.

This pointer exists for Copilot versions that do not yet read `AGENTS.md`
directly. Do not duplicate the shared rules here; keep them in `AGENTS.md` so
there is a single source of truth across every tool.

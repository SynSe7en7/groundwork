[% if has_ui %]import { EnvBadge } from "@/components/EnvBadge";
[% endif %]
export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-6 p-8">
[% if has_ui %]      <EnvBadge />
[% endif %]      <h1 className="text-3xl font-semibold tracking-tight">[[ project_name ]]</h1>
      <p className="max-w-prose text-center text-sm opacity-80">
        [[ project_description ]]
      </p>
      <p className="text-xs opacity-60">
        Edit <code className="rounded bg-black/5 px-1 py-0.5">app/page.tsx</code> to get started.
      </p>
    </main>
  );
}
